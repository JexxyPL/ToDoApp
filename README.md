My first project which is a simple To-Do app written in HTML CSS and Javascript packed into Electron .exe program.
To build it , first clone the repository, make sure electron-builder is installed via npm package and then run npx electron-builder --win

![Image Alt](https://raw.githubusercontent.com/JexxyPL/ToDoApp/refs/heads/main/screenshot.png)
